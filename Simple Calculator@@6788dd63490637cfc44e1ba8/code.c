#include <stdio.h>
int main() {
    int x,y,calculation;
    char op;
    scanf("%d %d %c",&x ,&y,&op);
    if(op=='+'){
        calculation =x+y;
    }
    else if(op=='-'){
        calculation = x-y;
    }
    else if(op=='')
    return 0;
}